{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "install": {
      "commands": ["cd backend && npm install"]
    }
  },
  "deploy": {
    "startCommand": "cd backend && npm start"
  }
}